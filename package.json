{
	"name": "next-blank",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"build": "next build",
		"deps:reset": "rm -rf node_modules .next bun.lock && bun install",
		"deps:show": "bun pm ls --depth=Infinity",
		"deps:upgrade": "bunx npm-check-updates -u && bun install",
		"dev": "bun server.js dev",
		"fix": "biome check --fix .",
		"format": "biome format --write .",
		"lint": "biome check .",
		"next:info": "next info 2>&1 | tail -n +2 > info.txt",
		"size": "du -k -d 1 . | sort -nr > size.txt",
		"start": "bun server.js start",
		"typecheck": "next typegen && tsc --noEmit",
		"unused": "bunx knip --no-exit-code 2>&1 | tail -n +2 > knip-report.txt"
	},
	"browserslist": [
		"> 0.5% in ID",
		"not dead",
		"not op_mini all",
		"baseline widely available"
	],
	"dependencies": {
		"next": "^16.3.4",
		"react": "^19.2.8",
		"react-dom": "^19.2.8"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.12",
		"@types/bun": "^1.4.1",
		"@types/react": "^19.2.18",
		"@types/react-dom": "^19.2.7",
		"typescript": "^7.0.2"
	}
}
